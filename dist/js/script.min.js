const images=Array.from({length:9},(e,t)=>`./images/${t+1}.jpg`),imageElement=document.getElementById("currentImg"),prevButton=document.getElementById("prevButton"),nextButton=document.getElementById("nextButton");let currentIndex=0;function updateImage(){imageElement.src=images[currentIndex],prevButton.disabled=0===currentIndex,nextButton.disabled=currentIndex===images.length-1}prevButton.addEventListener("click",()=>{0<currentIndex&&(currentIndex--,updateImage())}),nextButton.addEventListener("click",()=>{currentIndex<images.length-1&&(currentIndex++,updateImage())}),updateImage();